spring:
  application:
    name: eureka-client
  cloud:
    config:
      uri: http://localhost:8888
      name: eureka-client
      profile: https
management:
  endpoints:
    web:
      exposure:
        include: '*'