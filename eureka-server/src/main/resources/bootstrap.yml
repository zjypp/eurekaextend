spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8888
      label: master
      profile: https
      name: eureka-server
management:
  endpoints:
    web:
      exposure:
        include: '*'